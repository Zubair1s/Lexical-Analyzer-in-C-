This project is a simple Lexical Analyzer (Scanner) written in C++. It reads a source file and breaks it into tokens such as keywords, identifiers, numbers, operators, and symbols, then stores the results in a symbol table. simple and easy
 
 
